{
  "name": "cryptocop-payments",
  "version": "1.0.0",
  "description": "Microservice that validates credit cards that it receives from an order and prints out if it is valid or not on the console",
  "main": "index.js",
  "scripts": {
    "start": "nodemon -e js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Arnthor18@ru.is",
  "license": "MIT",
  "dependencies": {
    "amqplib": "^0.6.0",
    "card-validator": "^8.1.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.6"
  }
}
